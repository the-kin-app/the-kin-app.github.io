{
  "name": "kin-waitlist-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply kin-waitlist --local",
    "migrate:remote": "wrangler d1 migrations apply kin-waitlist --remote",
    "tail": "wrangler tail",
    "preview:email": "node scripts/preview-email.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
